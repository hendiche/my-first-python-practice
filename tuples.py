coordinates = (216, 10)  # tuples is like array but its absolute, cannot get change
# coordinates[1] = 9 #this code will return error, because tuples cannot get change
print(coordinates[1])

arrCoordinates = [coordinates, (9, 12), (80, 34)]
print(arrCoordinates)
